<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hshl.loginregistrierung">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature android:name="android.hardware.camera" android:required="true" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegActivity" android:label="@string/title_activity_reg" />
        <activity android:name=".Profil" android:label="@string/title_activity_profil" />
        <activity android:name=".Benachrichtigung" />
        <activity android:name=".Newsfeed" />
        <activity android:name=".TakePicture" android:exported="true" />
        <activity android:name=".Liste" />
        <activity android:name=".Bildhochladen" />
        <activity android:name=".Bildbearbeiten" />
        <activity android:name=".Benachrichtigungen" />
        <activity android:name=".Bildanzeigen" />
        <activity android:name=".Kommentar_schreiben"></activity>
        <activity android:name=".search"/>
    </application>

</manifest>